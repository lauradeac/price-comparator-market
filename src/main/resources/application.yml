spring:
  application:
    name: backend

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/price_comparator_market_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    open-in-view: false
logging:
  level:
    org.hibernate: WARN
